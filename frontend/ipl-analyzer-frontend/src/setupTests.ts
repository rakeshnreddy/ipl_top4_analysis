// import '@testing-library/jest-dom'; // if you want jest-dom matchers
// console.log('Test setup file loaded');

// To make jest-dom matchers available in Vitest:
import '@testing-library/jest-dom/vitest';
// You might also need to extend Vitest's expect if not done automatically by the above import
// import { expect } from 'vitest';
// import * as matchers from '@testing-library/jest-dom/matchers';
// expect.extend(matchers);

// Any other global setup can go here
// For example, if you need to mock other global objects or functions.
// console.log('Test setup file loaded with jest-dom matchers.');
